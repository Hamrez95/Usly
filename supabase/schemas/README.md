# Supabase schema source

The applied, ordered schema history lives under `supabase/migrations/`.

Target project:

- Name: `Usly`
- Project ref: `vgmahkbiwxklefxxdsix`

Never apply these migrations to another project without verifying its exact
project ref. The mobile app may contain only the project URL and publishable
client key; secret and `service_role` keys are forbidden.
